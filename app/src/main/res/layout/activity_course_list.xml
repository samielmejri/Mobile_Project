<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="#F7F5F5">

    <!-- Header TextView for Title -->
    <TextView
        android:id="@+id/textViewTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Welcome to the Courses"
        android:textSize="28sp"
        android:textStyle="bold"
        android:textColor="#413B61"
        android:layout_gravity="center_horizontal"
        android:paddingBottom="20dp" />

    <!-- Styled Add Course Button with Solid Background Color -->
    <Button
        android:id="@+id/buttonAddCourse"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Add a New Course"
        android:textSize="16sp"
        android:textStyle="bold"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="20dp"
        android:backgroundTint="#AE7DAC"
        android:textColor="#FFFFFF"
        android:elevation="6dp"
        android:padding="14dp"
        android:shape="rectangle" />

    <!-- Card Style RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewCourses"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingBottom="8dp"
        android:layout_marginTop="10dp" />
</LinearLayout>
